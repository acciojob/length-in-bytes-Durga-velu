const byteSize = (str) => {
  // write your code here
	if(str==ascii){
	return str.length;
	}
	else{
		return  byteSize.value;
	}
};


// Do not change the code below
const str =("Enter some string.");
(byteSize(str));
